<div>
  <h2>本当に退会しますか？</h2>
  <div>
    <p>退会する場合は、『退会する』をクリックしてください。</p>
  </div>
  <div>
    <%= link_to '退会しない', customer_path(@customer) %>
    <%#= link_to "退会する", withdraw_customer_path(@customer), method: :patch %>
    <%= form_with model: @customer, url: withdraw_customer_path(@customer), local: true do |f| %>
    <%= f.submit "退会", class: "btn btn-success" %>
    <% end %>
  </div>
</div>
