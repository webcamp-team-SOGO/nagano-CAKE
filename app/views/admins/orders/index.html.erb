<h1>ここは注文履歴の一覧画面　画面遷移元によってページが違う</h1>

<h4>注文履歴一覧</h4>


<table>
  <thead>
    <tr>
      <td>購入日時</td>
      <td>購入者</td>
      <td>注文個数</td>
      <td>注文ステータス</td>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
    <tr>
      <td><%= link_to "order.created_at.strftime('%Y/%m/%d %H:%M')", admins_order_path %></td>
      
      <td><%= order.customer.name %></td>

      <td><%= order.orderitems %></td>

      <td><%= order.status %></td>
    </tr>
    <% end %>
  </tbody>
</table>
